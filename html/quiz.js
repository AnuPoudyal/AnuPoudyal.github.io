"use strict";
/**
 * 
 * @param {number} number is number
 * @returns{number} is number
 */
function compute(number){
let num=prompt("enter the number separated by comma");
let sum=0;
let average;
for(let i=0;i<num;i++){
    sum=sum+i;
}
average=sum/num;
}
